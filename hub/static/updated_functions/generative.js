function appendGeneratedTaskToBody(params, payloads) {
    // Из params получаем taskType, lastTaskId и taskId (если режим редактирования)
    // Из payloads получаем заголовок задания, контент
    // Также вешаем обработчики событий
}

